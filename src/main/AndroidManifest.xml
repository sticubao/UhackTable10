<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.foodhack2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/official"
        android:label="@string/app_name"
        android:roundIcon="@drawable/official"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wtc"
            android:label=""
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".ingsearc" />
        <activity android:name=".wtcList" />
        <activity android:name=".SearchDishActivity" />

        <activity android:name=".MainActivity" />
        <activity android:name=".Procedures" />
        <activity android:name=".Process"></activity>
    </application>

</manifest>